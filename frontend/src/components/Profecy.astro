<!-- ═══════════════════════════════════════
         STATE: PROPHECY — Result display
    ═══════════════════════════════════════ -->
    <section id="state-prophecy" class="state-section hidden">
      <div class="prophecy-scene">


        <!-- Prophecy scroll -->
        <div class="prophecy-scroll">
          <div class="scroll-corner tl" aria-hidden="true"></div>
          <div class="scroll-corner tr" aria-hidden="true"></div>
          <div class="scroll-corner bl" aria-hidden="true"></div>
          <div class="scroll-corner br" aria-hidden="true"></div>

          <div class="scroll-inner">
            <p class="prophecy-intro cinzel">El Oráculo ha hablado:</p>
            <div class="prophecy-divider" aria-hidden="true">
              <span>— ✦ —</span>
            </div>
            <p id="prophecy-text" class="prophecy-text" role="status" aria-live="polite"></p>
            <span id="prophecy-cursor" class="cursor-blink" aria-hidden="true">|</span>
          </div>
        </div>

        <!-- Action buttons -->
        <div class="prophecy-actions">
          <button id="btn-again" class="oracle-btn oracle-btn--secondary cinzel" aria-label="Hacer otra consulta">
            <span class="btn-ornament" aria-hidden="true">⟳</span>
            <span class="btn-text">Consultar de nuevo</span>
          </button>
        </div>

      </div>
    </section>

<style>

  /* ── Prophecy ── */
  .state-section{
    margin-top: 1.5rem;
  }

  .prophecy-scene {
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    animation: fade-in-up 0.8s ease both;
  }

  @keyframes fade-in-up {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }



  .prophecy-scroll {
    padding: 2.5rem 2.8rem;
    width: 100%;
    max-width: 560px;
    background: linear-gradient(160deg, rgba(25, 18, 8, 0.95) 0%, rgba(15, 10, 4, 0.98) 100%);
    border: 1px solid var(--border);
    border-radius: 2px;
    position: relative;
    box-shadow:
      0 0 60px rgba(0,0,0,0.7),
      0 0 30px rgba(201,168,76,0.05),
      inset 0 0 80px rgba(0,0,0,0.4);
  }

  .prophecy-scroll::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 50% 0%, rgba(201,168,76,0.05) 0%, transparent 50%);
    pointer-events: none;
    border-radius: 2px;
  }

  /* Scroll corner ornaments */
  .scroll-corner {
    position: absolute;
    width: 18px;
    height: 18px;
    border-color: var(--gold-dark);
    border-style: solid;
    opacity: 0.6;
  }
  .scroll-corner.tl { top: 8px;  left: 8px;  border-width: 1px 0 0 1px; }
  .scroll-corner.tr { top: 8px;  right: 8px; border-width: 1px 1px 0 0; }
  .scroll-corner.bl { bottom: 8px; left: 8px;  border-width: 0 0 1px 1px; }
  .scroll-corner.br { bottom: 8px; right: 8px; border-width: 0 1px 1px 0; }

  .scroll-inner {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .prophecy-intro {
    font-size: 0.65rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--text-muted);
    text-align: center;
  }

  .prophecy-divider {
    text-align: center;
    color: var(--gold-dark);
    font-size: 0.8rem;
    letter-spacing: 0.3em;
  }

  .prophecy-text {
    font-style: italic;
    font-size: clamp(1.05rem, 2.5vw, 1.25rem);
    line-height: 1.85;
    color: var(--gold-pale);
    text-align: center;
    letter-spacing: 0.02em;
    min-height: 4rem;
  }

  .cursor-blink {
    color: var(--gold);
    font-style: normal;
    animation: cursor-flash 0.8s step-end infinite;
    font-size: 1.1em;
  }

  .cursor-blink.hidden { display: none !important; }

  @keyframes cursor-flash {
    0%, 100% { opacity: 1; }
    50%       { opacity: 0; }
  }

  .prophecy-actions {
    display: flex;
    justify-content: center;
    gap: 1rem;
  }
</style>